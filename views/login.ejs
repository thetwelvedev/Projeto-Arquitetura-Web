<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Acesso ao Sistema</title>
    <style>
        :root {
            --primary-color: #007bff; /* Azul vibrante */
            --success-color: #28a745; /* Verde para sucesso */
            --danger-color: #dc3545; /* Vermelho para erro */
            --text-color: #343a40;
            --bg-color: #e9ecef;
            --card-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex; 
            justify-content: center; 
            align-items: center; 
            height: 100vh; 
            margin: 0;
            background-color: var(--bg-color); /* Fundo suave */
            color: var(--text-color);
        }

        .login-box {
            background: white; 
            padding: 40px; 
            border-radius: 12px; 
            box-shadow: var(--card-shadow); 
            width: 350px; /* Largura um pouco maior */
            transition: transform 0.3s ease;
        }

        .login-box:hover {
            transform: translateY(-5px); /* Efeito sutil ao passar o mouse */
        }

        h2 {
            text-align: center;
            color: var(--primary-color);
            margin-bottom: 25px;
            font-weight: 600;
        }

        label {
            font-weight: 500;
            display: block; /* Garante que a label fique acima do input */
            margin-bottom: 5px;
        }

        input[type="email"], input[type="password"] { 
            width: 100%; 
            padding: 12px; 
            margin-bottom: 20px;
            border: 1px solid #ced4da;
            border-radius: 6px; 
            box-sizing: border-box; 
            transition: border-color 0.3s;
        }

        input[type="email"]:focus, input[type="password"]:focus {
            border-color: var(--primary-color);
            outline: none; /* Remove o contorno padrão */
            box-shadow: 0 0 0 0.1rem rgba(0, 123, 255, 0.25);
        }

        button { 
            background-color: var(--primary-color); 
            color: white; 
            padding: 15px 20px; 
            margin-top: 10px;
            border: none; 
            cursor: pointer; 
            width: 100%; 
            border-radius: 6px; 
            font-size: 16px;
            font-weight: 600;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }

        button:hover {
            background-color: #0056b3; /* Azul mais escuro no hover */
            box-shadow: 0 0.2rem 0.5rem rgba(0, 0, 0, 0.2);
        }

        .message-error {
            color: var(--danger-color); 
            background-color: #f8d7da;
            border: 1px solid var(--danger-color);
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 15px;
            text-align: center;
        }
        
        .message-success {
            color: var(--success-color); 
            background-color: #d4edda;
            border: 1px solid var(--success-color);
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 15px;
            text-align: center;
        }

        .link-cadastro {
            margin-top: 20px; 
            text-align: center;
            font-size: 14px;
        }
        
        .link-cadastro a {
            color: var(--primary-color);
            text-decoration: none;
        }
        
        .link-cadastro a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <div class="login-box">
        <h2>Acesso ao Sistema</h2>

        <% if (typeof erro !== 'undefined' && erro) { %>
            <p class="message-error">
                <% if (erro === 'usuario_nao_encontrado') { %>
                    Usuário não encontrado.
                <% } else if (erro === 'senha_incorreta') { %>
                    Senha incorreta.
                <% } else if (erro === 'limite_brute_force') { %>
                    <!-- CORREÇÃO AQUI: Trata o erro de Rate Limiting -->
                    Muitas tentativas de login. Por segurança, aguarde 1 minuto para tentar novamente.
                <% } else { %>
                    Por favor, faça o login para continuar.
                <% } %>
            </p>
        <% } %>
        
        <% if (typeof sucesso !== 'undefined' && sucesso === 'cadastro') { %>
            <p class="message-success">
                Cadastro realizado com sucesso! Faça seu login.
            </p>
        <% } %>

        <form action="/login" method="POST">
            
            <label for="email">Email:</label>
            <input type="email" placeholder="nome@ufrr.edu.br" name="email" required>

            <label for="senha">Senha:</label>
            <input type="password" placeholder="Sua Senha" name="senha" required>
            
            <button type="submit">Acessar</button>
        </form>

        <p class="link-cadastro"><a href="/register">Não tem cadastro? Cadastre-se aqui.</a></p>
    </div>

</body>
</html>